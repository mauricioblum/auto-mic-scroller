const recordBtn = document.getElementById("start-stop");

recordBtn.addEventListener("click", () => {
  sendStartStopMessage();
});

function sendStartStopMessage() {
  // TODO: Add play/pause funcionality
}
